spring:
    application:
        name: job-processing-system

    data:
        mongodb:
            uri: mongodb://localhost:27017/job_processing

    kafka:
        bootstrap-servers: localhost:9092

server:
    port: 8080

management:
    endpoints:
        web:
            exposure:
                include: health,info,metrics